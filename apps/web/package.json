{
  "name": "@openbot/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "build:desktop": "tsc --noEmit -p tsconfig.json && vite build --mode desktop",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run src --passWithNoTests --maxWorkers=4",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:demo": "tsc --noEmit -p tsconfig.json && vite build --config vite.demo.config.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "1.7.5",
    "@modelcontextprotocol/sdk": "1.30.0",
    "@openbot/domain": "0.0.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "@vitejs/plugin-react": "6.1.1",
    "jsdom": "30.0.1",
    "vite": "8.2.2"
  }
}
