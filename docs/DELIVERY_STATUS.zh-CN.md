# 可用 Agent 交付状态

[English](DELIVERY_STATUS.md) · [简体中文](DELIVERY_STATUS.zh-CN.md)

这是 2026 年 9 月 8 日交付分支的实际状态，不是公开发布公告。代码见
[PR #19](https://github.com/yxflc11/openbot/pull/19)，实施与验证见[工作记录](DELIVERY_WORKLOG.zh-CN.md)。
旧[路线图](ROADMAP.md)保留此前阶段；本页用于核对这次完成的功能与缺口。

## 本次交付

| 项目 | 已实现 | 验证与实际限制 |
| --- | --- | --- |
| 三端 Desktop | macOS arm64 DMG、Windows x64 用户级 EXE、Linux x64 AppImage/DEB；清单、SHA-256、CI 产物与草稿发布门槛 | 原生构建与本地 DMG 挂载/ASAR/fuse 通过。公开 Desktop Release 尚未发布，不声称已签名或完成设备认证。见[下载安装](DESKTOP_INSTALLATION.zh-CN.md)。 |
| 命令安装 | 固定版本 shell/PowerShell 脚本、保留系统信任检查、有界校验下载、Linux 失败重试 | 真实 Bash 和 PowerShell 失败/重试测试通过；公开安装命令仍需对应 Desktop Release 发布后才能使用。 |
| 模型配置 | 私有持久化加密配置，OpenAI/Anthropic/OpenRouter，桌面和 Web 设置入口 | 真实配置持久化与官方 SDK 夹具通过；OpenRouter 公开元数据读取成功。未验证付费推理；Server 仍只有一份活动配置，未做按 Bot 配置或任意端点。 |
| Agent 执行 | 员工角色上下文、有界模型/工具/观察循环、频道读取、持久回复和任务状态 | Server/SDK/数据库及实际界面通过；原生模型循环尚未接入可交互 Worker 工具。 |
| 资料与报告 | 最多 3 个任务明确 HTTPS 地址、有界文本提取、带来源的 Markdown、鉴权下载与原生独占保存 | 真实界面/Server/数据库/磁盘，模型与来源使用夹具。本机代理 DNS 将公网地址映射到保留网段，真实公网读取成功尚未验证。没有通用搜索、PDF 或任意文件访问。 |
| 运行控制 | 持久化停止、明确创建新任务的重新提交、模型报告 Token 数和具体失败原因 | 停止/完成数据库竞争与实际界面通过；不是断点续跑、流式输出、账单统计或 Worker 停止功能。 |
| 经验记忆 | 成功任务候选经验、Owner 编辑/批准/拒绝、默认关闭的模型共享、修订绑定和撤销 | 实际桌面/Web 审核、使用、撤销、刷新通过；排除私有和其他 Bot 记忆。没有自动批准活动记忆或可执行 SKILL.md 正文。 |
| 桌面连续使用 | 替换、导航、Server 切换、渲染进程退出和窗口关闭时中止旧事件流 | 实际构建 ASAR 连续完成 4 个刷新间隔的不同报告任务，再刷新 4 次。使用独立测试身份与对话框路径，不等同于已签名应用认证。 |

## 代码功能库与 Hermes 对照

以下是当前代码审查结果，不应全部计为这四小时新增的功能。

| 能力 | 当前真实状态 | 尚未完成 |
| --- | --- | --- |
| 定时自动化 | `automations.ts`、`postgres-automation-store.ts` 已有 15 分钟至 7 天间隔、暂停/恢复/删除和持久化派发 | 自然语言建计划、cron/时区语义、外部平台投递、多 Server 协调 |
| 技能库与迁移 | `employee-package.ts` 与领域类型已有版本化技能元数据、审核、依赖、受控导入/导出 | 标准 SKILL.md 正文加载、受限调用、来源、执行中撤销与实际验收任务 |
| 消息网关 | 运行时没有 Telegram/Discord/Slack/WhatsApp/Signal 网关适配器 | 先完成一个平台，接入 Server 身份/审批、入站幂等和授权回复 |
| 委派与并行 | 原生 runner 可同时运行最多两个任务；没有隔离子 Agent 协议 | 父子权限、隔离上下文、取消、预算、汇总、恢复；独立任务并发不等于委派 |
| 电脑交互 | Docker Provider 能按明确网址导航并返回 PNG；Cua/Lume/Coder 只有声明，没有 execute 实现 | 交互工具、受限一次性授权、已批准副作用、独占接管和真实设备测试 |
| 多运行环境 | 已有 Server 容器与 Worker 路由，没有 SSH/Daytona/Modal/Singularity 适配器 | 按研究复用流程选择一个后端，验证生命周期、隔离与清理 |
| TUI 与对话回溯 | Web/Desktop 频道和有界近期上下文 | 完整终端交互、全文/语义检索、后台整理、Honcho 类用户建模 |
| 训练/研究导出 | 已有运行事件与审计记录 | 脱敏轨迹导出、可重放格式、质量筛选和保留策略；审计日志不等于训练集 |

学习方向明确借鉴 [Hermes Agent](https://github.com/NousResearch/hermes-agent/tree/63279301bcbdc185c1b07b98a9312eb0c862f26d)。
本次完成的是需要人工审核的记忆闭环，尚未达到 Hermes 全功能对齐。

## 接下来按此顺序验收

1. **真正公开安装：**补齐捆绑 PostgreSQL 原生依赖与源码对应、LGPL 源码/重新链接证据，
   再进行维护者发布审查、main CI 和公开安装下载。补签名、公证与干净设备安装/升级/卸载证据。
   目前继续明确提供开发产物入口。
2. **真实可用任务：**在合适网络中，用真实支持的模型和公网资料完成报告、停止与经验复用，
   将夹具证据升级为真实使用证据。
3. **可执行技能：**先完成审核正文加载与受限调用，再把技能库称为执行库，沿用现有审核和撤销权限。
4. **受控电脑操作：**先选一个平台/Provider，完成一次获批交互任务，再扩大平台声明。
   参见 [Provider 验证](PROVIDER_CONFORMANCE.zh-CN.md)。
5. **生态：**先一个消息网关，再按实际工作流补委派或另一个后端，不再堆只有声明的适配器。

macOS 当前可运行本地 Server；Windows/Linux 是远程客户端。退出桌面会停止应用托管的本地服务；
无人值守自动任务需要持续在线的 Server。自动更新、完整备份恢复、三端本地 Server 均未完成。
