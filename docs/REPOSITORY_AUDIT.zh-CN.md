# 仓库软件工程审查

[English](REPOSITORY_AUDIT.md)

日期：2026-09-10。范围：OpenBot 整个 monorepo，包含应用、共享包、Provider、文档、迁移、打包和 CI，不包含其他 GitHub 仓库。这是工程与可阅读性审查，不保证所有代码分支和外部服务都已验证。

## 判断

仓库具备通用开源项目的基本条件：MIT 许可、上游 notice、标准 MCP 扩展入口、类型化 Provider 契约、贡献模板、复用记录和可执行检查。检查到的共享包没有反向导入应用层；Server 权威、生成文件和私有数据边界明确。

可阅读性已落后于功能增长。主要问题是架构文档陈旧、缺少模块导航、CI 遗漏重要数据库集成套件，以及多代界面样式叠加。文件多本身不是问题；没有证据的大规模重命名或重写会增加回归风险。

## 发现与处理

| 优先级 | 问题 | 处理与状态 |
| --- | --- | --- |
| P1 | PostgreSQL CI 只有 automation/direct，协作和新交互测试可能默认跳过 | 增加独立 collaboration/interactions CI 步骤和临时数据库。本地四套 PostgreSQL 共 49 项通过；推送后仍需观察托管 CI。 |
| P1 | 架构文档混合早期 fork 候选和当前实现，把来源消息唯一、AG-UI、游标等当成现状 | 重写当前英文架构并提供中文翻译，明确复合唯一键、REST/SSE 快照、扩展权限和运行限制；没有复制一份陈旧架构制造重复。 |
| P2 | Server/Web 缺少目录级说明，贡献者不知道改哪里 | 增加双语仓库地图及两个模块 README，列出职责、入口和定向测试。 |
| P2 | 贡献教程使用宽泛 Node/npm 版本和 `npm install`，与 CI/锁文件不一致 | 同步双语教程，使用 Node 22.22.2 CI 基线、npm 10.9.9 和 `npm ci`；其他 Node 版本必须满足 engines。 |
| P2 | Web 多层历史全局 CSS 会因选择器顺序覆盖局部修复 | 新消息操作/回应已独立组件化；全局样式清理与实际窗口验收仍属于整体集成，不宣称所有 CSS 已清理。 |
| P2 | Server `app.ts`/`postgres-store.ts`/`native-agent.ts` 与 Web `App.tsx`/`ChannelWorkspace.tsx`/`api.ts` 较大 | 新增交互、追加指令和流处理使用独立模块；其他拆分应按功能边界渐进并配回归，不宣称已完成全面拆分。 |
| P2 | 历史里程碑文档和多份 README 容易随版本漂移 | 当前架构、地图与功能文档明确边界；根 README/官网翻译一致性仍需发布复核，research 保留为有日期的决策历史。 |

## 已有的软件工程基础

- 严格 TypeScript、精确 optional 字段、索引检查和运行时 schema。
- 有序迁移清单与数据库前缀历史校验；任务/审批/审计事务提交。
- 权限、取消、Provider 和并发负面测试；确定性 SDK 测试明确区别模拟与付费服务证据。
- 固定版本 CI、依赖与密钥扫描；生成产物和用户数据不纳入源码。
- 清晰的 Server、Node、Client、Provider 职责和每 Bot 插件授权；接口声明不冒充执行支持。
- 双语贡献规范、issue/PR 模板、功能文档及上游归属。

## 验证和剩余工作

本审查运行了文档链接检查、26 文件迁移检查、消息操作/回应组件测试、Server/Web 类型检查、Server 回归和四套隔离 PostgreSQL 测试。后续发布集成已通过完整根目录 `npm run check`、演示渲染检查和独立官网部署。托管 CI 的安全、PostgreSQL、两种容器架构和 macOS 均已通过；Windows 原生安装生命周期及最终发布仍需完成，不能由本审查替代。

扩展模块前按[仓库地图](REPOSITORY_MAP.zh-CN.md)定位。优先增加独立功能模块，避免在无关协调器继续堆分支。替代实现通过回归前保留旧行为；删除文件或样式必须根据引用和渲染证据，不能只凭名称陈旧。
