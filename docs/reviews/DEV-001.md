# DEV-001 security review verification

- Status: Active
- Verified base: `origin/main` at `750bffde340dc44a02e440693b23675ecf4ad4ad`
- Verification date: 2026-09-04
- Reviewer report: external DEV-001 follow-up supplied in the task handoff
- Chinese translation: [DEV-001.zh-CN.md](DEV-001.zh-CN.md)

## Repository state

The canonical base contains the merged cross-platform Employee and portability slice. Repository
evidence confirms Server-owned Employee profiles, reviewed skills, typed memory, evolution events,
portable package review/import, per-Node enrollment and revocation, versioned Provider capability
routing, bounded realtime transport, artifact verification, migration drift checks, and bounded
shutdown. The unchanged base passes documentation, research, and migration-manifest checks. A full
baseline check could not continue to lint in the fresh worktree because dependencies were not yet
installed (`biome: command not found`).

The same evidence also keeps the product pre-alpha. Cua, Lume, and Coder are declaration-only;
native desktop Providers, proof-of-possession identity, capability leases, hardened browser egress,
native keyrings, continuous takeover, executable-skill scanning, registry distribution, ownership
transfer, and the optional Office plugin lifecycle are not production capabilities.

## Finding matrix

The status values are intentionally limited to **Confirmed**, **Partial**, **False positive**,
**Already fixed**, and **Deferred-with-contract**. A confirmed finding is not automatically safe to
fix without the research and protocol work described below.

| ID | Status | Verified evidence and disposition |
| --- | --- | --- |
| H1 | Confirmed | `apps/server/package.json` does not depend on `@openbot/policy`. `apps/server/src/run-dispatcher.ts` forwards Node-supplied `action`, `target`, and `risk` directly to `requestApproval`; `packages/policy/src/index.ts` is not called. Existing `packages/policy/src/index.test.ts` tests only the detached evaluator. Add Server-authoritative classification/policy tests in `apps/server/src/run-dispatcher.test.ts`. |
| H2 | Deferred-with-contract | `RunDispatcher.resolveApproval` and `NodeRegistry.resolveApproval` send only `approval.resolved`; `apps/node/src/client.ts` resumes its waiter from that message. Neither side revalidates `targetFingerprint`, and the protocol has no signed, TTL-bound, single-use lease. `docs/API.md` and `docs/ARCHITECTURE.md` already disclose the limitation. Keep real side-effecting Providers disabled until a new protocol/ADR covers issuance, signature/key lifecycle, exact run/node/provider/action/target binding, expiry, consumption, restart semantics, revocation, and replay tests. |
| H3 | Partial | `apps/node/src/client.ts` accepts `OPENBOT_NODE_CREDENTIAL` as a long-lived bearer injection path. The default `FileNodeCredentialStore` already uses opened-handle checks, rejects symlinks and unsafe POSIX mode, and has `apps/node/src/credential-store.test.ts`; Server storage is digest-only. `docs/NODE_ENROLLMENT.md` explicitly discloses that proof of possession, rotation, and native keyrings are absent. Research native adapters and PoP/mTLS before changing the identity protocol; do not relabel file permissions as device identity. |
| H4 | Confirmed | The broad claim is correct: `OwnerAuthService` limits an in-memory map and `app.ts` supplies the browser Origin as the client key, so all clients at one Origin share a bucket and restarts clear it. The narrower `"unknown"` claim is a false-positive path in normal routing because mutation middleware rejects a missing/untrusted Origin before login. `owner-auth.test.ts` covers only the in-process bucket; `app.test.ts` covers Origin rejection. Replace the bucket with an atomic durable store and derive a deployment-aware network client identity with an explicit trusted-proxy contract. |
| M1 | Partial | Skipping browser Origin on `/api/v1/nodes/enroll` is intentional because Worker Hosts are not browser clients. `node-identity.ts`, `postgres-node-identity-store.ts`, `node-identity.test.ts`, and `app.test.ts` already prove Node-id binding, 60–3,600 second TTL, one-time exchange, digest-only storage, replay rejection, request bounds, and an `enrolled` audit event. A stolen token plus its Node id can still be redeemed remotely and the redemption event has no safe source metadata. Reuse the trustworthy client-identity boundary from H4 for pseudonymous audit/rate controls; never persist or log the raw token. |
| M2 | Confirmed | `providers/docker/src/index.ts` resolves the requested host before sending the original URL to a separate browser service. It neither pins the browser connection to that address nor checks redirect hops, so DNS rebinding and redirects bypass the precheck. `providers/docker/src/index.test.ts` covers only the initial lookup. Do not claim SSRF protection; require an enforceable isolated-egress contract or keep the adapter limited to trusted test targets. |
| M3 | Confirmed | `.github/workflows/ci.yml` runs `npm ci`, repository checks, and PostgreSQL verification with read-only repository permissions, but has no production dependency audit or secret scanner. Add pinned, read-only, non-uploading checks and repository tests/configuration for the selected scanner. |
| M4 | Confirmed | `apps/node/package.json` lists Cua, Lume, and Coder declaration-only workspaces as runtime dependencies, while `apps/node/src/providers.ts` imports and executes only Docker. `apps/node/src/providers.test.ts` imports the declarations solely for conformance tests. Move declaration packages to development-only dependencies while preserving the Provider SDK extension boundary and accurate docs. |
| M5 | Confirmed | `FileNodeCredentialStore` has no Windows ACL or DPAPI/Credential Manager enforcement and intentionally skips POSIX checks on Windows. `credential-store.test.ts` has no Windows-specific protection test. Keep Windows production support unclaimed until a researched native adapter, migration behavior, and real-Windows evidence exist. |
| M6 | Confirmed | `apps/node/src/client.ts` sends a truncated raw `Error.message` in `run.failed`; `RunDispatcher` persists it through `failRun`, and artifact failures also persist a local raw exception message. The protocol bounds size but does not classify or redact. Add stable public error classes/codes, Server-side normalization independent of Node honesty, redaction tests, and local-only detailed diagnostics. |
| M7 | Confirmed | `OPENBOT_LOG_LEVEL` is parsed in `packages/config/src/index.ts` but unused. Server and Node use raw `console.*`; Hono's default logger lacks a redaction contract and stable run/node/request fields. Add a small structured, level-aware logger or a pinned maintained dependency, with tests proving secret redaction and correlation fields. |
| L1 | Confirmed | `apps/web/src/components/ApprovalCard.tsx` and `LoginScreen.tsx` have no component tests. Add interaction, pending/disabled, error, and accessibility-relevant assertions using the existing Web test stack. |
| L2 | Already fixed | `packages/office-plugin` is a separate workspace; `apps/web` neither imports nor depends on it. Its manifest is `deferred`, and `README.md`, `README.zh-CN.md`, `docs/ROADMAP.md`, `docs/INTERFACE.md`, and ADR-0013 accurately describe isolation. Do not expand its UI in DEV-001. |
| L3 | Partial | `sensitive-content.ts` detects private-key markers, AWS access-key ids, GitHub token prefixes, credential assignments, bearer tokens, and local paths; `sensitive-content.test.ts` covers only a subset. Compare maintained secret-scanner pattern sources, expand only high-confidence cases, and avoid calling heuristic coverage complete. M3's repository scanner and application-content heuristics remain separate boundaries. |
| L4 | Partial | `serverEnvSchema` requires 12 characters and rejects the example value; it has no composition rules, which avoids outdated complexity policy. `owner-auth.test.ts` does not cover password configuration. Reassess minimum length and breached-password guidance against current NIST/OWASP material together with H4; document that throttling and TLS remain independent controls. |
| L5 | Confirmed | Dispatcher background failures end in `reportDispatchError`, which calls only `console.error`. No safe `run_events` record or metric is written. Add bounded classified audit data with run/node/phase correlation and prove persistence failures cannot recurse or expose raw exceptions. |

## Fix order and security contracts

1. H1: make the Server recompute policy and minimum risk before persistence; deny unknown actions
   and reject a Node's lower-risk claim.
2. H4: make login throttling atomic and durable; use direct peer identity by default and honor
   forwarding metadata only from explicitly trusted proxies.
3. M3: add pinned, read-only production dependency and secret-scanning CI checks.
4. M1, M6, M7, and L5: reuse the client-identity and structured-error/logging boundaries instead of
   creating parallel trust rules.
5. H2, H3, M2, and M5: proceed only after complete fail-closed protocol or platform designs. The
   current disclosure and disabled/development claim is safer than an unverifiable partial control.

Every behavioral fix requires a completed research record and reuse-ledger update before code.
This audit must be updated with commits, exact tests, residual risks, and final verification output
before DEV-001 is closed.
