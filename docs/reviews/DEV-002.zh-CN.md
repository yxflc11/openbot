# DEV-002 Server 容器审查与执行检查点

[English](DEV-002.md) · [简体中文](DEV-002.zh-CN.md)

- 日期：2026-09-05
- 范围：Node 24 Server 容器里程碑、README 翻译，以及部署、数据库、启动/停机、CI 和权限边界。
- 基线：`0a5b72dcf8c50d7a2328898706b3329eee8abc4b`，即已验证的 PR #14 合并提交。
- 已审查实现：`1e25e7c`；翻译：`65d01a2`；部署说明：`7df4863`。
- 审查结论：下列修正完成后，本里程碑没有剩余阻断项。本轮为自动化代码库审查，覆盖改动和邻接
  关键路径，并以全仓检查佐证；不代表全面的外部安全审计。

## 已交付与验证

确认 PR #14 为绿色并完成合并。[PR #15](https://github.com/yxflc11/openbot/pull/15) 提供固定
Node `24.20.0` LTS 多架构基线、npm `10.9.9` 构建工具、裁剪后的 Server 生产依赖、非 root
运行、migration、健康身份检查、Compose 只读根、私有临时存储、持久对象卷，以及一致的 20 秒
停机预算。新增九项契约测试与 amd64/arm64 原生容器任务，并补齐英文、中文调研、复用和部署
文档，以及完整日语、巴西葡萄牙语 README。

[运行 33938120773](https://github.com/yxflc11/openbot/actions/runs/33938120773) 的九项检查
全部通过：安全、全仓检查、数据库、Linux/Windows/macOS 兼容性、Windows Worker Host 构建，
以及两条原生 Server 容器任务。两个容器日志分别证明七个工作区编译成功，以及包含全部 18 条
migration 的完整冒烟成功。本地 `npm run check`、局部格式化、Compose 解析、Docker 构建
检查和真实 arm64 冒烟也通过。

已通过 computer-use 打开 Cursor 并提交有界审查任务；账号提示额度耗尽，没有产生代码。
实现与独立复核由 Codex 完成。已核实既有的每周 Node 26 LTS 迁移自动化，没有重复创建；它要求
官方正式进入 Active LTS 并完成迁移证据审查后才准备 PR。

## 实现和审查中已关闭的问题

| 问题 | 修正与证据 |
| --- | --- |
| 旧镜像低于 engine 下限、标签可变、以 root 运行且包含开发包 | 固定 Node 24 index digest，分离构建/生产依赖，显式复制运行文件，并检查依赖清单与非 root 身份 |
| npm exec 吞掉 Turbo 参数 | 明确使用 `npm exec --`；原生构建证明裁剪路径和 Server 专属构建正确 |
| Turbo prune 遗漏根 TypeScript 配置 | 构建层显式复制 `tsconfig.base.json`，七个必需工作区全部编译通过 |
| Compose 与贡献说明仍要求已移除的共享 Node token | 删除旧变量和说明，并加入拒绝回退的 fixture |
| 冒烟断言了不存在的健康响应字段 | 探针和冒烟改为验证实际 `ok` 与 `openbot-server` 身份，真实运行通过 |
| Compose 默认停机时间与 Server HTTP 排空冲突 | Compose 和冒烟均明确使用 20 秒，并加入负向契约测试 |
| 冒烟中的限制没有落到 Compose | 配置只读根和同样的私有、有界临时文件系统；CI 实际解析 Compose |
| 缺少配置的探测可能无限运行，migration 数量断言不够精确 | 限时失败探测，并对照完整 journal 数量验证 |
| 新增脚本格式漂移未被 lint 发现 | 显式执行并通过局部 Biome 格式检查；未声称修复已有无关警告 |

## 残余风险与后续工作

镜像通过了指定托管验证，但尚未发布或提升平台支持等级。`/health` 是启动后的存活检查，不是
持续数据库就绪。启动期信号、繁忙 dispatcher 排空和数据库关闭卡住仍缺完整生命周期证据。
旧 root 对象卷需要运维审查迁移；Compose 原始数据库密码拼接需要单独测试的编码修正。
PostgreSQL digest 固定、镜像 OS 漏洞扫描、发布 SBOM/来源证明、备份恢复与升级回滚验收仍未完成。

按既有 [G0–G10 验收计划](../EXECUTION_PLAN.zh-CN.md)，G0、G1 已在各自声明的证据等级完成，
**还剩九道关卡：G2–G10。** G2–G5 已有部分实现，但仍需 Provider 专属场景与受控设备、安装、
密钥库证据；G6–G10 分别对应设备持有证明、可执行 Skill 隔离、受治理的检索/提案、分发/所有权
和真实电脑控制。G11 继续延期。各关卡工作量差异很大，不能把数量换算为产品完成百分比。

下一项建议实施 G2 的 Docker/browser 有界一致性套件：只用回环 fixture 服务，把现有适配器
接入现有一致性 runner；覆盖鉴权失败、导航/截图边界、畸形响应、取消和清理；输出严格的
hermetic 报告，并让必需场景失败阻断 CI。新增行为前继续复用和复核已有 runner 与上游适配器
契约。fixture 通过只能证明密闭测试，不能证明真实浏览器或桌面平台。本 Server 容器 PR 没有
实现这项可选后续工作，也没有将其标为完成。
