# 目标模式执行计划

[English](EXECUTION_PLAN.md) · [简体中文](EXECUTION_PLAN.zh-CN.md)

## 目标

按依赖安全的顺序交付 OpenBot 下一阶段基础：先证明当前安全基线可靠，再建立可重复的
Windows、macOS、Linux 证据；随后交付原生工作主机与受保护的本机凭证，替换可复制的 Node
身份；然后加入可执行技能、受治理的记忆和分发；最后才开放真正的桌面控制。

Server 始终是员工身份、授权、路由、审批和审计的唯一权威。工作主机、Provider、模型、网页、
导入技能和消息始终视为不可信。办公室可视化继续作为延期的可选插件。

## 为什么必须按这个顺序

```text
安全基线
   |
   v
三平台托管 CI --> 可重复的真实设备证据
   |                       |
   +-----------+-----------+
               v
      原生工作主机 + 系统密钥库
               |
               v
        Node 持有证明身份
               |
       +-------+-------+
       v               v
可安全执行的技能     受治理的记忆
       +-------+-------+
               v
       注册表与所有权转移
               |
               v
       真实输入与桌面接管
```

越靠后的阶段拥有越大的权限和影响范围。文档声明、模拟 fixture 或能力上报都不能替代下面的
证据门槛。

## 状态说明

- **已完成：**已经在所述证据等级实现并验证。
- **进行中：**当前正在执行的有界切片。
- **受阻：**需要明确的用户授权或外部基础设施。
- **已计划：**顺序已确定，但尚未开始。
- **延期：**明确不属于本目标。

## 执行波次

### 波次 A——先建立可信反馈

| 编号 | 状态 | 切片 | 为什么现在做 | 交付物与好处 | 退出条件 |
| --- | --- | --- | --- | --- | --- |
| G0 | 进行中；等待授权 | 发布并观察加固后的基线 | 本地通过不能证明 GitHub 的干净环境也能完成安装、构建、测试和扫描。 | 得到远程、可审核的安全基线；之后出现的问题可以确定来自新增工作，而不是未知起点。 | 本地检查保持绿色；获得明确推送授权后推送分支，并观察全部现有 CI。它不代表授权创建 PR、合并或发布。 |
| G1 | 本地进行中；等待远程证据 | Windows/macOS/Linux 托管 CI 矩阵 | 没有跨平台反馈就开发跨平台功能，会积累路径、Shell、权限和运行时差异。 | 每次修改都在明确的三平台 runner 上检查可迁移协议、Node、Provider SDK、配置和 Web。 | 固定 runner 系列和 Node 版本；三平台确定性测试通过；文档明确托管 CI 不等于真实设备支持。 |
| G2 | 本地进行中；待具体测试集与真实设备证据 | Provider 一致性 runner 与真实设备证据契约 | 托管虚拟机不能证明服务恢复、密钥库、GUI 权限、硬件隔离或桌面行为。 | 用一套可重复场景和有界证据格式，让平台声明可以复现，而不是靠口头判断。 | 有密闭负向 fixture；报告绑定目标并会过期；产物不含秘密；真实设备报告写明 OS/版本/架构/硬件；不能自我认证。 |

### 波次 B——让工作主机可安装、可相信

每个平台都要把系统密钥库与服务一起交付，避免让运维人员把长期凭证放进普通配置文件。公共
凭证存储接口还必须能够保存未来的持有证明私钥。

| 编号 | 状态 | 切片 | 为什么按此顺序 | 交付物与好处 | 退出条件 |
| --- | --- | --- | --- | --- | --- |
| G3 | 本地进行中；压缩包和休眠来源证明工作流已实现 | Linux 工作主机、安装器、systemd、Secret Service | Linux 的 daemon 和打包路径最直接，适合用最小的平台特有面积确定公共生命周期。 | 先签名压缩包，再提供 deb/rpm；专用服务账号；安装、启动、停止、升级、回滚可预测；凭证受保护。它会成为其他平台的参考实现。 | systemd 生命周期和恢复测试；x64/arm64 证据；权限或密钥库失败时安全关闭；不开放公网入站控制端口。 |
| G4 | 已计划 | Windows 工作主机、安装器、Service、Credential Manager | Windows 的服务身份、ACL、安装器和 Session 隔离差异较大，应该在公共生命周期确定后解决。 | 提供签名安装器、可恢复的 Windows Service，并让凭证离开明文文件；企业部署与卸载可审计。 | 先取得真实 Windows x64 证据；ARM64 未测试前不声明支持；服务账号 ACL 测试；安装/升级/卸载/回滚；Credential Manager 拒绝时安全关闭。 |
| G5 | 已计划 | macOS 工作主机、安装器、launchd、Keychain | macOS 可以复用主机协议，但要额外解决签名、公证、Keychain access group、launchd 和隐私权限。 | 提供签名、公证的安装包和 launchd 服务，以 Keychain 保存凭证，并给出真实的权限诊断。 | 取得所声明 Mac 架构的真实设备证据；安装/升级/卸载/回滚；Keychain 锁定和 entitlement 缺失的负向测试；此时仍不声明桌面控制。 |
| G6 | 已计划 | Node 持有证明、mTLS、轮换、撤销与防重放 | 原生服务让被盗 bearer token 的价值更高；可复制凭证仍能冒充工作主机时，不能开放真实输入。 | Node 用设备私钥证明身份；连接双向认证；凭证可轮换；失陷 Node 可吊销；截获消息不能重放。 | 保留单次登记；系统允许时使用不可导出密钥；有界挑战应答；短期凭证；明确轮换重叠规则；防重放测试；Server 权威审计；时钟/存储/网络失败时安全关闭。 |

### 波次 C——扩展员工能力，但不导入权限

| 编号 | 状态 | 切片 | 为什么现在做 | 交付物与好处 | 退出条件 |
| --- | --- | --- | --- | --- | --- |
| G7 | 已计划 | 可执行 Agent Skills 隔离检查与沙箱安装 | Skill 是不可信代码，不只是说明性元数据；没有检查和隔离就分发或学习技能并不安全。 | 展示完整归档清单和文件 diff，检查来源/许可证/签名/恶意内容，在隔离环境验证，显式授权并支持回滚审计；员工可以扩展，但不会静默安装任意代码。 | 路径穿越、软链接、解压膨胀、可执行内容、依赖、验证器、扫描器和沙箱故障全部安全关闭；检查不在 Server 进程运行；激活仍是独立 Owner 操作。 |
| G8 | 已计划 | 记忆检索、保留策略与员工技能提案 | 自动检索和自主提案会扩大数据访问和主动行为，必须建立在审核与沙箱边界之上。 | 带可见来源地检索相关记忆；执行保留与删除策略；员工可以提议但不能自行批准技能。减少重复配置，同时保留 Owner 控制。 | 提示注入测试；限定范围的检索；过期/删除任务；版本恢复；无正文审计；提案会过期/取代并必须由 Owner 审核；继续注明 Hermes Agent 启发来源。 |
| G9 | 已计划 | 员工注册表、公开分发、选择性复制与所有权转移 | 分发会把包或权限错误放大到更多人和机器，所以必须晚于安全包、技能、记忆策略和 Node 身份。 | 支持版本化发现与更新、发布者信任/撤销、选择性数据复制和双方认证转移收据；团队可以复用员工，但不会复制机器权限或全部私人记忆。 | 注册表记录仍由 Server 权威管理；签名且不可变的包版本；回滚/撤销；接收端创建新的本地绑定；转移后不能让双方继续持有同一权限；完成隐私审查。 |

### 波次 D——开放高影响电脑控制

| 编号 | 状态 | 切片 | 为什么最后做 | 交付物与好处 | 退出条件 |
| --- | --- | --- | --- | --- | --- |
| G10 | 已计划 | 连续桌面、独占接管与真实写入/点击 Provider | 它是影响最高的能力：既可能泄露屏幕，又能修改外部系统；需要前面全部身份、策略、审计、安装和证据边界。 | 有界画面传输、短期查看权限、人与 Agent 互斥控制、紧急停止，以及遵循 prepare/approve/commit 的窄 Windows/macOS/Linux Provider。OpenBot 最终可以操作无 API 软件，Owner 仍保有控制权。 | commit 前复核目标指纹；单次 capability lease；审批前绝不输入；重定向/窗口变化安全失败；同一时间只有一个控制者；立即撤销/停止；有界产物；每个 Provider 和 OS 都有真实设备一致性证据。 |
| G11 | 延期 | 办公室可视化插件 | 它改善展示，不解决安全或执行基础；底层员工/Run 模型仍在变化时开发会浪费资源。 | 延期可减少返工，让核心系统独立可用。 | 只有在 G10 核心流程成熟且另有明确里程碑要求时重新评估。 |

## 每个切片的执行契约

每项非简单工作严格使用同一顺序：

1. 定义一条可观察的验收路径，以及权限、数据、副作用边界。
2. 搜索 GitHub、正式标准和平台第一方文档。
3. 比较维护状态、版本、测试、Issue、平台适配、安全边界和许可证，并固定审查的版本或提交。
4. 在改变行为或依赖之前，完成或新增 `docs/OPEN_SOURCE_REUSE.md` 条目和调研记录。
5. 实现最小、失败关闭、输入输出有界的纵向切片。
6. 按风险加入正常、负向、并发/生命周期和故障路径测试。
7. 更新英文原版文档，并同步面向用户的简体中文文档。
8. 运行针对性测试和 `npm run check`；涉及数据库、安装、安全或真实设备时再运行对应门槛。
9. 创建一个聚焦提交。完成一个切片只是检查点，不能用来跳过下一个依赖。

## 证据和支持声明规则

- 模拟 fixture 只能证明协议契约。
- 托管 CI runner 只能证明代码在该 runner 镜像上的构建与测试兼容。
- 密闭集成只能证明指定 Provider 和环境。
- 真实设备报告必须写明目标、OS 版本、架构、硬件、Provider 版本、场景版本、时间和可复现证据。
- `Supported` 和 `Certified` 必须有维护者审核的真实设备与安全证据。
- 凭证、权限、签名、扫描器、密钥库、时钟、存储或策略数据缺失时必须安全关闭，不能静默降低权限。

## 当前检查点

- DEV-001 本地安全基线已经完成：Server 权威审批策略、持久限流、依赖/历史凭证扫描、脱敏结构化
  日志、安全错误与派发审计，以及 Web 交互测试均已在本地验证。
- 当前分支包含多个领先于 `origin/main` 的聚焦目标提交，本目标尚未推送它们。
- G1 的 Linux x64、Windows x64 与 macOS arm64 明确矩阵已经在本地实现，并由失败关闭的仓库
  规则检查。三个远程任务全部被观察前，它仍只是未验证的远程配置，不代表平台支持。
- G2 的严格场景 runner 和真实设备报告契约已经在本地实现。测试覆盖确定性生命周期顺序、取消
  时限、cleanup 失败、不可信结果拒绝、秘密抑制、预期失败过期、必需设备元数据，以及不覆盖的
  私有证据文件。具体 Provider 测试集和受控真实设备报告仍待完成。
- G3 已实现两套明确 systemd 配置和有界 Linux Secret Service 适配器。测试覆盖不回退的精确
  选择、工具 stdin、超时/输出限制、缺失/错误结果、错误身份、写后验证和服务单元加固。第一段压缩
  包实现现在能从干净源码暂存未签名 x64/arm64 候选目录，包含哈希固定的 Node 运行时、白名单 ncc
  输出、仅生产依赖的 SPDX SBOM、规范 manifest 和校验和。Ubuntu 24.04 打包器会重新验证候选、
  固定 GNU tar/XZ 行为、记录系统包修订、拒绝覆盖，并输出确定性的未签名压缩包及伴随文件。两次
  模拟 Ubuntu 24.04 amd64 构建已经逐字节一致，并通过 SHA-256/XZ 完整性校验；这只是打包证据，
  不是真实主机支持声明。仅 tag 工作流现在也已在本地实现，包含精确 Action 固定值、最小权限、
  main 祖先/SemVer 门禁、两次构建比较、构建与 SBOM attestation，以及临时原始审查上传。它还没
  有远程运行，也不能创建 GitHub Release。矩阵现在会把 x64 和 arm64 分配给对应 CPU 的托管
  runner，并要求包内 Node 与应用在证明前完成符合协议、无额外权限的本地握手。x64 路径已在本地
  Ubuntu 容器模拟下通过；两个原生托管任务仍未观察。获授权的来源证明观察、安装器事务，以及真实
  x64/arm64 systemd 和密钥库锁定/解锁证据仍按 ADR-0033 执行。
- G0 受外部授权约束：推送必须得到 Owner 明确授权。创建 PR、合并、发布和修改仓库设置是另外的
  操作，目前均未获授权。
- 等待 G0 决定期间，可以继续仓库内的计划、调研与验证；但对应远程证据未观察前，不能宣称托管
  矩阵或平台支持已经成立。
