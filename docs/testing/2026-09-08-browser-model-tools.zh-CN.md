# 浏览器与模型工具验收 — 2026-09-08

[English](2026-09-08-browser-model-tools.md) · [简体中文](2026-09-08-browser-model-tools.zh-CN.md)

员工浏览器、模型服务和共享公开联网工具在推送前通过了下列检查。本文记录
`feat/cross-platform-employees` 分支在 `5fdd99ced126509e702fc4ad00314bf06f3d50c3` 基础上的功能改动，
更新了较早[基线报告](2026-09-08-kimi.zh-CN.md)中的实现状态。包含本文的提交标识本次验证的源码。

## 最终本地检查

主机为 macOS arm64，Node.js 为 `v22.23.2`。数据库使用一次性的 `postgres:17.11-bookworm`，
回环端口为 `55439`，数据目录位于内存，数据库名称以 `_test` 结尾。

| 检查 | 结果 |
| --- | --- |
| `npm run check` | 通过：文档、研究检查用例、迁移清单、lint、类型、测试与构建。Turbo 复用了有效缓存；仅 PR 事件运行的研究检查在本地跳过。 |
| `npm run test -- --force` | 通过：49 个文件、352 项测试；23 个 Turbo 任务全部无缓存执行。其中 Server 228 项、Web 50 项、协议 29 项、Node 16 项。 |
| 隔离数据库上的 `npm run db:verify` | 通过：19 个迁移、并发及幂等迁移启动、现有控制面检查、模型 Run 归属与认领、原子回复、上下文隔离与限额、恢复、连接加密持久化、版本、停用访问、不含内容的工具审计及连接并发容量。 |
| `git diff --check` | 通过。 |

## 功能证据与范围

- 真实浏览器流程和员工持久化配置在 macOS Docker 承载的固定版本 Linux ARM64 Chromium 上通过。
  桌面和手机界面的证据与范围见[员工浏览器](../EMPLOYEE_BROWSER.zh-CN.md)。
- 全部 12 个已注册预设均有工具调用续接契约测试：11 个 OpenAI 兼容预设（含自定义）及原生 Anthropic。
  测试覆盖仅在内存保留不透明推理状态、凭据路由、输入输出限额、校验、取消与错误脱敏。
- 真实 Kimi 检索桥接完成一次 Formula 搜索和两次对话调用，返回可读证据与来源链接，未含加密标记。
  详情见[共享联网工具研究](../research/shared-model-web-tools.zh-CN.md)。
- Tavily 和其他对话供应商有契约测试；没有授权密钥的账户不宣称通过真实 API 验收。
  浏览器证据不表示原生桌面 Provider 或自主多步浏览通过认证。

验证后清理临时测试服务与文件。凭据、私有对话及本地截图不纳入提交。
本地检查通过不表示已部署生产环境，也不表示 GitHub 托管 CI 已通过。
