# 可用 Agent 交付工作记录

[English](DELIVERY_WORKLOG.md) · [简体中文](DELIVERY_WORKLOG.zh-CN.md)

从 main `8868da1` 开始，起点为 2026-09-07 23:54（Asia/Shanghai）。用户要求至少持续工作四小时。
记录实际改动与按项目集中验证的结果，时间经过本身不代表功能完成。

| 顺序 | 项目 | 验收结果 | 当前状态 |
| --- | --- | --- | --- |
| 1 | 下载、安装、模型配置 | 固定版本下载、安装入口、配置重启保留 | 本地检查与 9 项 CI 全部通过；公开发布待完成 |
| 2 | 工具任务与文件交付 | 按任务范围读取资料，产出可下载报告 | 已实现；完整检查、12 项数据库测试和实际下载通过 |
| 3 | 执行体验 | 员工上下文、可理解状态、停止/恢复与用量限制 | 已实现；完整检查、16 项数据库测试与实际停止/重提/用量显示通过 |
| 4 | 知识进入执行 | 已审核技能与记忆参与任务，保留来源及审核边界 | 已实现经验审阅与记忆使用闭环；可执行技能正文待完成 |
| 5 | 生态扩展 | 消息平台、隔离委派、更多执行环境 | 核心使用路径之后进行 |

第一项实现期间为 23:54–次日 00:35。已补三端安装器适配、源提交/校验值门槛、草稿发布流程、
macOS/Linux 与 Windows 命令安装脚本、四语 README 下载入口、中英安装说明，以及私有模型目录
初始化与持久化。`npm run check` 已通过，实际 macOS DMG 也通过只读挂载、ASAR/fuse、原生
可执行文件与许可文件检查。第一次 DMG 验证发现应用嵌套一层，已修复并将此检查加入安装器流程。

首次验证通过的本地 DMG 为 191,019,942 字节，SHA-256：
`3c42b0d5cf8824352615245c5b6ce003786bd2607b7e718b411222aa4678fd6c`。
这不代表已完成公开发布、签名、公证或真实设备安装认证；原生依赖与源码对应仍需核对。
模型测试使用隔离夹具，没有发送付费请求。

00:35–00:59：修复三端 CI 中 npm Electron 缓存缺少许可文件的问题，改为从 Packager 实际解压的
运行时保留许可。ead6c8f 的 CI 34144512831 共 9 项全部通过，包括三端安装器和两种架构容器。

00:45–01:22：完成任务明确网址读取、Markdown 报告事务发布、鉴权下载和共享文件卡片。
完整检查、12 项一次性 PostgreSQL 集成测试通过；生产依赖漏洞审计为 0。实际构建界面连接真实
Server/数据库，完成登录、提交任务、下载中文文件名报告、来源核对、未登录 401、刷新保留，
以及 1280×900 / 390×844 布局检查，页面错误为 0。模型和来源响应采用确定性夹具。
界面验收发现并修复网址紧接中文标点时的解析问题。

真实公网来源测试尚未通过：本机 DNS 把 example.com、rfc-editor.org 解析到 198.18.* 保留网段，
读取器按规则在连接前拒绝；不放宽该边界。任意本地文件、PDF、Worker 文件交付仍不在本项范围。

01:24–01:33：复核原生壳发现通用下载被禁止，补上仅接收产物 UUID 的主进程保存入口，
通过鉴权下载有界 Markdown，系统对话框选择新文件，独占创建并拒绝覆盖。完整检查通过；
真实 Electron 验证渲染器、preload、主进程、Server 到文件的链路，来源内容匹配，重复保存
保留原文件。测试仅拦截系统对话框的路径选择，不声称完成手工系统对话框认证。

01:34–02:02：完成员工角色上下文、持久化停止、明确创建新任务的重新提交、模型报告用量和
固定失败原因。完整检查、16 项 PostgreSQL 测试通过。实际界面验证角色、300/240 用量、
模型执行中停止、新任务与原历史保留，以及窄屏布局，页面错误为 0。

P2 凭证扫描仅命中拒绝测试中的合成网址；改为运行时构造输入并整理两个未合并提交，扫描规则
保持不变。离线完整历史扫描通过；268063c 的 CI 34149581888 共 9 项全部通过。

02:02–02:29：完成默认关闭的模型记忆使用开关、有界快照、成功任务候选经验、全文编辑/批准/
拒绝、来源与修订审计。完整检查通过；启用 22 项真实 PostgreSQL 集成场景后，Server 共 273 项
测试通过。实际界面验证批准后的经验进入下一任务、私有和已撤销记忆不发送、刷新保留与宽窄屏
布局，页面错误为 0。未调用付费模型，也不宣称自主技能学习。

P3 提交 4a39d5c 的 CI 34149990564 共 9 项全部通过。

02:29–02:49：接入固定版本 OpenRouter 3.0.0、仅元数据的密钥/模型验证、明确路由限制和已有
任务生命周期/用量。补上 Web 侧栏模型设置入口并保留未发送草稿。完整检查通过，生产依赖漏洞
为 0；实际界面验证保存、刷新、密钥不回显和宽窄屏。公开模型元数据 GET 返回 200，推理仍使用
夹具。P4 提交 74d7819 的 CI 34151847897 共 9 项全部通过。

- 18:49–19:12 UTC：补齐安装失败恢复与 Windows 流式下载限制。整仓检查通过；真实 Linux
  测试覆盖复制失败清理、重试和并发目录保留。PowerShell 7.5.0 的 9 个离线夹具通过，
  覆盖重定向拒绝及次数、读取限长、已有文件保留，以及响应头前和正文传输中的取消。
  Windows 原生 CI 同时运行 Windows PowerShell 和 pwsh 测试。
- OpenRouter 提交 078bba1 的 CI 34153185480 已通过全部 9 项任务。
